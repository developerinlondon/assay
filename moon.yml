# Workspace root. Per-project moon.yml under crates/* owns actual tasks
# (see crates/assay/moon.yml for the workspace-wide cargo runners).
# This file exists so moon's project graph resolves cleanly.

type: "library"
language: "rust"
