# machinectl-clone requires `from`; this one omits it.
[template]
id = "bad-rootfs-clone-no-from"
display_name = "Clone missing from"
packages = []

[template.rootfs]
source = "machinectl-clone"
# from omitted on purpose
